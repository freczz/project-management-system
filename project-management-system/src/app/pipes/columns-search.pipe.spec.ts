import ColumnsSearchPipe from './columns-search.pipe';

describe('ColumnsSearchPipe', (): void => {
  it('create an instance', (): void => {
    const pipe: ColumnsSearchPipe = new ColumnsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
